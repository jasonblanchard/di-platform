apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
bases:
- ../base
configMapGenerator:
- literals:
  - TEST_PASSWORD=vault:secret/data/di-authn/development/app#TEST_PASSWORD
  - JASON_PASSWORD=vault:secret/data/di-authn/development/app#JASON_PASSWORD
  - JWT_SECRET=vault:secret/data/di-authn/development/app#JWT_SECRET
  - SESSION_SECRET=vault:secret/data/di-authn/development/app#SESSION_SECRET
  name: authn-config
  namespace: di
  behavior: merge
