apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: workspace-web
  name: workspace-web
  namespace: di
spec:
  replicas: 1
  selector:
    matchLabels:
      app: workspace-web
  template:
    metadata:
      labels:
        app: workspace-web
    spec:
      serviceAccountName: workspace-web
      containers:
      - image: di-workspace-web:e2e7954ea2ab380e1ba84d74c3c004370832cf99 # {"$ref":"#/definitions/io.k8s.cli.substitutions.image"}
        name: workspace-web
        envFrom:
        - configMapRef:
            name: workspace-web-config
        livenessProbe:
          httpGet:
            path: /health
            port: 8080
        readinessProbe:
          httpGet:
            path: /health
            port: 8080
        ports:
        - containerPort: 8080
